document.addEventListener('DOMContentLoaded',()=>{const ids=LS.get('bigmart_wishlist',[]),items=PRODUCTS.filter(p=>ids.includes(p.id));if(!items.length){$('#wishlistPage').innerHTML='<div class="empty" style="grid-column:1/-1"><div style="font-size:55px">♡</div><h2>Your wishlist is empty</h2><p>Save products you love for later.</p><a class="btn primary" href="products.html">Explore Products</a></div>'}else renderProducts(items,'#wishlistPage')})