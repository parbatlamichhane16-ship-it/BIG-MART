function checkoutRender(){const items=cartData();if(!items.length){$('#checkoutPage').innerHTML='<div class="empty">Your cart is empty. <a class="btn primary" href="products.html">Shop now</a></div>';return}const u=currentUser();if(!u){location.href='login.html?next=checkout.html';return}let subtotal=items.reduce((s,p)=>s+p.price*p.qty,0),coupon=Number(sessionStorage.getItem('bm_coupon_discount')||0),delivery=subtotal>=2000?0:100,total=subtotal+delivery-coupon;let addresses=LS.get('bigmart_addresses_'+u.email,[]);if(!addresses.length)addresses=[{id:1,name:u.name,phone:u.phone,city:'Kathmandu',area:'New Road',details:'Demo delivery address'}];let address=addresses[0];$('#checkoutPage').innerHTML=`<div class="steps"><div class="step active">1. Address</div><div class="step active">2. Delivery</div><div class="step active">3. Payment</div><div class="step active">4. Review</div></div><div class="checkout-layout"><div><div class="checkout-card"><h3>Delivery Address</h3><div class="address-option selected"><b>${address.name}</b><p>${address.phone} · ${address.city}, ${address.area}</p><small>${address.details}</small></div><a href="addresses.html" class="text-link">Manage addresses →</a></div><div class="checkout-card" style="margin-top:15px"><h3>Delivery Method</h3><label class="payment-option selected"><input type="radio" name="delivery" checked> Standard Delivery · Rs. ${delivery}</label><label class="payment-option"><input type="radio" name="delivery"> Express Delivery · Rs. 200</label></div><div class="checkout-card" style="margin-top:15px"><h3>Payment Method</h3><label class="payment-option selected"><input type="radio" name="pay" value="Cash on Delivery" checked> Cash on Delivery</label><label class="payment-option"><input type="radio" name="pay" value="eSewa"> eSewa (Demo)</label><label class="payment-option"><input type="radio" name="pay" value="Khalti"> Khalti (Demo)</label><label class="payment-option"><input type="radio" name="pay" value="Card"> Debit/Credit Card (Demo)</label><small style="color:var(--muted)">Payment is simulated because this project uses frontend-only HTML/CSS/JS.</small></div><button class="btn primary full" onclick="placeOrder()">Place Order · ${money(total)}</button></div><aside class="checkout-summary"><h3>Order Summary</h3>${items.map(p=>`<div class="mini-line"><span>${p.name} × ${p.qty}</span><b>${money(p.price*p.qty)}</b></div>`).join('')}<hr><div class="mini-line"><span>Subtotal</span><b>${money(subtotal)}</b></div><div class="mini-line"><span>Coupon</span><b>−${money(coupon)}</b></div><div class="mini-line"><span>Delivery</span><b>${delivery?money(delivery):'FREE'}</b></div><div class="sum-row total"><span>Total</span><b>${money(total)}</b></div></aside></div>`}
function placeOrder(){const items=cartData(),u=currentUser();if(!u)return;let subtotal=items.reduce((s,p)=>s+p.price*p.qty,0),coupon=Number(sessionStorage.getItem('bm_coupon_discount')||0),delivery=subtotal>=2000?0:100,total=subtotal+delivery-coupon,payment=document.querySelector('input[name=pay]:checked')?.value||'Cash on Delivery',orders=LS.get('bigmart_orders',[]),order={id:'BM-'+new Date().getFullYear()+'-'+String(Date.now()).slice(-6),date:new Date().toISOString(),email:u.email,items:items.map(p=>({id:p.id,qty:p.qty})),total,payment,status:'Order Placed',address:{name:u.name,phone:u.phone,city:'Kathmandu',area:'Demo Address'}};orders.unshift(order);LS.set('bigmart_orders',orders);LS.set('bigmart_cart',[]);sessionStorage.removeItem('bm_coupon_discount');LS.set('bigmart_notifications_'+u.email,[{id:Date.now(),title:'Order placed successfully',text:'Your order '+order.id+' has been placed.',read:false,date:new Date().toISOString()},...LS.get('bigmart_notifications_'+u.email,[])]);location.href='order-success.html?id='+order.id}
document.addEventListener('DOMContentLoaded',checkoutRender)